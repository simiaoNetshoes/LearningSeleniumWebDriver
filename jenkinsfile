pipeline{
    agent any

    stages{


      def mvnHome = tool 'maven-3.5.4'
      stage("Build package") {
           withMaven(
             maven: 'maven-3.5.3'){
             }
           sh "cd $mvnHome/bin/mvn clean test"
         }
    }
}